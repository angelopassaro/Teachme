Modifiche:
Relazione manage in course sostituita con lesson al posto di price.
Relazione teache in student sostituita con lesson al posti di price.
(Eliminare ciò che resta di price)



Todo:
/*BACK END*/
Delete cascade (https://github.com/strongloop/loopback-datasource-juggler/issues/88).

passpartout hasOne(in student)viene creato usando le API di student. +1
passpartout belongsTo(in passpartout)viene creato usando le sue API ma bisogna definire un hook(before-save) per associarlo a uno student esistente.

Student definire un hook(before-save) per associarlo a un università esistente.
Aggiungere Feedback generico per calcolo 

Aggiungere controlli email e lunghezza caratteri stringhe.

/*Front END*/
Aggiungere pagine contact e about
Aggiungere CSS responsive.



